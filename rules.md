# Project Rules

- Avoid browser-based checks by default.
- Only run browser checks when they are truly necessary and cannot be replaced by faster local verification.
- Prefer code review, linting, unit/integration tests, and CLI validation before any browser automation.
